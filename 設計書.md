# 避難所レイアウトツール 設計書

> 2026-05-11 オーバーホール版 / 仕様書とあわせて読むこと

## 1. 概要

避難所レイアウトツールを、独立した 3 つのツールの集合に再構成する。

- **避難所レイアウト**: 施設図面の上に区画を配置するツール（従来機能）
- **区画エディタ**: 区画と区画リストを編集・保存するツール（新規）
- **区画印刷**: 区画リストから印刷用 HTML を生成するツール（新規）

初期画面はメニュー画面で、そこから 3 つのツールに遷移する。

## 2. アーキテクチャ全体像

### 2.1 ルーティング

`react-router-dom` v7 の `HashRouter` を使用。GitHub Pages の静的ホスティング制約（直リンクで 404 になる問題）を回避するため。

```text
/#/             メニュー画面
/#/layout       避難所レイアウト
/#/editor       区画エディタ
/#/print        区画印刷
```

各ツール画面には「メニューに戻る」ボタンを配置する。終了ボタンは設けない。

### 2.2 永続化

| データ                             | 保存先                     | 理由                                             |
| ---------------------------------- | -------------------------- | ------------------------------------------------ |
| 共有区画リスト（画像 base64 含む） | IndexedDB (`idb-keyval`) | LocalStorage の 5MB 上限を超える可能性が高いため |
| ユーザー設定（スナップ等）         | LocalStorage               | 小さく頻繁に読まれるため                         |
| 避難所レイアウトの背景画像         | IndexedDB (`idb-keyval`) | 画像バイナリのため（仕様継続）                   |
| 配置済み区画情報・縮尺             | LocalStorage               | 軽量。次回起動時の自動復元用                     |

旧バージョンの LocalStorage / IndexedDB のキーは無視し、新フォーマットでクリーンスタートする。

### 2.3 ディレクトリ構成

```text
src/
  main.tsx                    エントリ
  App.tsx                     <HashRouter> + <Routes>
  pages/
    MenuPage.tsx              "/"
    LayoutPage.tsx            "/layout"
    EditorPage.tsx            "/editor"
    PrintPage.tsx             "/print"
  tools/
    layout/                   避難所レイアウト固有のコンポーネント
    editor/                   区画エディタ固有のコンポーネント
    print/                    区画印刷固有のコンポーネント
  shared/
    types.ts                  Zone / ZoneList / Layout の型定義
    zoneListIO.ts             .list.json の入出力
    layoutIO.ts               .layout.json の入出力
    storage.ts                IndexedDB / LocalStorage ラッパー
    components/               共通コンポーネント（ヘッダー、戻るボタン等）
    settings.ts               ユーザー設定の型と既定値
  styles/                     グローバルスタイル
```

### 2.4 言語

**TypeScript** を採用。長期メンテナンスを見据え、型による安全性と IDE 補完を確保する。`.jsx` → `.tsx`、`.js` → `.ts` に移行する。

## 3. UI ライブラリ採用方針

### 3.1 採用ライブラリ

**Mantine v8** を採用。

### 3.2 採用理由

| 評価軸                                         | Mantine | MUI | shadcn/ui | Chakra UI | Vanilla CSS |
| ---------------------------------------------- | ------- | --- | --------- | --------- | ----------- |
| 一般ユーザー向けの親しみやすい既定デザイン     | ◎      | ○  | ○        | ○        | △          |
| TypeScript ファースト                          | ◎      | ○  | ◎        | ○        | -           |
| バンドルサイズ（tree-shaking 後）              | ○      | △  | ◎        | ○        | ◎          |
| ダイアログ・モーダル・通知・数値入力等の網羅性 | ◎      | ◎  | △        | ○        | ×          |
| 設定パネル向け Form コンポーネントの充実       | ◎      | ○  | ○        | ○        | ×          |
| ライセンス                                     | MIT     | MIT | MIT       | MIT       | -           |

**決め手:**

1. メニュー画面・3 ツール画面・設定パネル・縮尺入力・印刷設定など、フォームとモーダルが多用される構成のため、Mantine の標準コンポーネント（`Modal`, `NumberInput`, `Select`, `ColorInput`, `FileInput`, `Stack`, `Group` 等）の網羅性が最も適合する。
2. MUI と比較してバンドルサイズが軽く、デザインも柔らかく一般ユーザー向きと判断。
3. shadcn/ui は Tailwind 前提でコピペ運用のため、本ツール規模では構築コストが見合わない。
4. Vanilla CSS 継続は、3 ツール × ダイアログ多数の構成で再発明コストが大きく、メンテナンス性で不利。

### 3.3 デザイントークン

Mantine の `MantineProvider` で、本ツール独自のプライマリカラー（青系 `#2196f3` 継承）と日本語向けフォントスタックを設定する。ライト/ダーク切替は当面なし。

## 4. データモデル

### 4.1 区画 (Zone)

```typescript
type ZoneType = {
  id: string;                  // UUID v4 推奨
  name: string;                // 例: "受付"
  width: number;               // メートル単位
  height: number;              // メートル単位
  color: string;               // 区画自体の塗り潰し色（#RRGGBB）
  resizable: boolean;          // 個別指定可
  /** 配置・印刷時に区画表示名を表示するか。未定義は true 扱い */
  showName?: boolean;
  /** 区画名と区画表示名を同じにするか。未定義は true 扱い。false なら displayName を使用 */
  displayNameSameAsName?: boolean;
  /** displayNameSameAsName が false のときに区画上へ描画される表示名。未定義は '' 扱い */
  displayName?: string;
  /** 表示名の文字色 (#RRGGBB)。未定義は '#000000' */
  nameColor?: string;
  /** 表示名のフォントサイズ（メートル単位、テキスト高さ）。未定義は自動（区画寸法に追従） */
  nameFontSize?: number;
  /** 表示名の横位置。未定義は 'center' */
  nameAlignH?: 'left' | 'center' | 'right';
  /** 表示名の縦位置。未定義は 'middle' */
  nameAlignV?: 'top' | 'middle' | 'bottom';
  /** 枠線を描画するか。未定義は true 扱い */
  showBorder?: boolean;
  /** 枠線色 (#RRGGBB)。未定義は '#333333' */
  borderColor?: string;
  /** 枠線の太さ（区画エディタ画面上での px）。未定義は 1 */
  borderWidthPx?: number;
  image?: {
    dataUrl: string;           // base64 画像（"data:image/png;base64,..."）
    fitMode: 'fill' | 'contain' | 'manual'; // 区画サイズに合わせる / 縦横比維持: 長辺フィット / 縮尺設定
    scaleX?: number;           // fitMode === 'manual' のとき有効（区画サイズに対する比、1.0 = ぴったり、0.1 刻みのボタン入力対応）
    scaleY?: number;
    rotation: number;          // -180 ～ 180、1° 刻み
  };
};
```

未定義フィールドは `getZoneBorder()` / `getZoneDisplayText()` / `getZoneNameColor()` / `getZoneNameAlignH()` / `getZoneNameAlignV()` ヘルパーでデフォルト値（[src/shared/types.ts](src/shared/types.ts) を参照）を補完。`.list.json` の旧バージョンとの後方互換のため。

### 4.2 区画リスト (`.list.json`)

```json
{
  "version": 1,
  "name": "標準区画リスト",
  "zones": [ /* ZoneType[] */ ]
}
```

- 拡張子: `.list.json`
- 画像は base64 で埋め込み（採用: Q9 案 A）
- 区画リストファイルを読み込むと、避難所レイアウトと区画印刷の区画一覧を置き換える

### 4.3 配置済み区画 (PlacedZone)

避難所レイアウト画面で配置された個別インスタンス。

```typescript
type PlacedZone = {
  id: string;                  // 配置 ID
  typeId: string;              // ZoneType.id
  x: number;                   // メートル単位
  y: number;
  rotation: number;            // 度
  // 個別リサイズの結果（resizable=true の場合のみ）
  width?: number;              // 上書き時のみ
  height?: number;
};

type TextBlock = {
  id: string;
  kind: 'text';
  text: string;
  x: number;
  y: number;
  fontSize: number;
  scaleX: number;
  scaleY: number;
  rotation: number;
  color: string;
};
```

座標・寸法は **メートル単位** で保持し、表示時のみ縮尺と viewScale を掛け算する。これにより縮尺再設定時にも既存配置が破綻しない（現状の px ベース実装の改善）。

### 4.4 レイアウトファイル (`.layout.json`)

```json
{
  "version": 1,
  "scaleRatio": 50,
  "background": {
    "dataUrl": "data:image/png;base64,...",
    "width": 1920,
    "height": 1080
  },
  "zoneList": { /* ZoneList 全体 */ },
  "placed": [ /* (PlacedZone | TextBlock)[] */ ]
}
```

- 拡張子: `.layout.json`
- 背景画像も base64 で埋め込み（採用: Q7）
- 使用した区画リストを丸ごと内包する（後日再現性確保のため）

### 4.5 ユーザー設定

```typescript
type UserSettings = {
  snap: {
    enabled: boolean;          // 既定 true
    sizeMeters: number;        // 既定 0.1（=10cm）
  };
};
```

LocalStorage キー: `evac-tool/user-settings`。

### 4.6 印刷ツールの設定永続化

```typescript
// 印刷ツール固有: 用紙設定と縮尺のみ永続化
type StoredPrintSettings = {
  paper: PaperSetting;   // size, orientation, customWidthCm, customHeightCm
  scale: ScaleSetting;   // drawingLength, drawingUnit, printLengthCm
};
```

LocalStorage キー: `evac-tool/print/settings`。マージン・枚数・配置方式は永続化対象外（毎回ユーザー設定する前提）。

### 4.7 ストレージキー一覧

| キー | 場所 | 内容 |
| --- | --- | --- |
| `evac-tool/zone-list` | IndexedDB | 共有区画リスト（最後に保存されたもの） |
| `evac-tool/background` | IndexedDB | 避難所レイアウトの背景画像 |
| `evac-tool/user-settings` | LocalStorage | スナップ設定 |
| `evac-tool/layout/state` | LocalStorage | 避難所レイアウトの縮尺と配置済みアイテム |
| `evac-tool/print/settings` | LocalStorage | 区画印刷の用紙設定と縮尺 |

「デフォルトに戻す」操作はそのツールが扱うキー（および共有キー `zone-list`）をクリアする。

## 5. 各ツールの仕様

### 5.1 メニュー画面 (`MenuPage`)

- タイトル「避難所レイアウトツール」と短い説明文を表示
- 3 つのツールをアイコン + 名称 + 説明の大きめのカードで配置
- 一般ユーザー向けの親しみやすいデザイン（Mantine の `Card` コンポーネント）

### 5.2 避難所レイアウト (`LayoutPage`)

- 従来機能を継承（画像読込、縮尺設定、ドラッグ配置、回転、リサイズ、ズーム、パン、PNG 保存）
- 追加機能:
  - 区画リストファイル (`.list.json`) 読み込み: 警告ダイアログ「配置されている区画が全て削除されますがよろしいですか？」→ OK で既存配置をクリアし、リストを置換
  - レイアウト結果保存: `.layout.json` で背景画像含めて出力（accept フィルタは `.layout.json`）
  - レイアウト読み込み: `.layout.json` から復元
  - 区画リスト初期化（デフォルトに戻す）: 配置済みも含めてクリア
  - ユーザー設定アクセス（ツールバーの「設定」ボタンから共通モーダルを開く）
- カスタム区画作成機能は **削除**（区画エディタへ統合）
- スナップ（ユーザー設定に従う）を配置・移動・リサイズ全てに適用
- **テキストブロック** のフロー: 「テキスト」ボタン → 配置先クリック → モーダルで文字列・フォントサイズ・**文字色** を入力して配置。既存テキストはダブルクリックで編集モーダルが開き、内容・サイズ・色を変更可能。
- 区画タイプに画像があれば、レイアウト上でも色塗り → 画像 → 枠線 → 文字 の順で重ねて描画する（[src/shared/components/ZoneImageKonva.tsx](src/shared/components/ZoneImageKonva.tsx) を経由）

### 5.3 区画エディタ (`EditorPage`)

- 区画リストを編集する独立ツール
- 機能:
  - 区画の追加・削除・複製
  - 区画ごとに以下を編集:
    - 名称、サイズ（m、スナップ適用）、塗り潰し色、リサイズ可否
    - **区画表示名関連**:
      - **区画表示名を表示する** (`showName`) — 区画上にテキスト描画するか
      - **区画名と区画表示名は同じ** (`displayNameSameAsName`) — OFF にすると区画名と別の `displayName` を指定可能
      - **表示名の文字色** (`nameColor`)
      - **文字サイズの自動調整 / 手動指定** (`nameFontSize`、メートル単位)
      - **横位置 / 縦位置** (`nameAlignH` / `nameAlignV`、各 3 値の SegmentedControl)
    - **枠線の有無 / 色 / 太さ (px)** — 白基調の区画など輪郭が見えにくい場合の補助
    - 画像、画像表示モード、画像縮尺、画像回転
  - 画像表示モード: ドロップダウン (`区画サイズに合わせる` / `縦横比維持: 長辺フィット` / `縮尺設定`)
  - 画像縮尺 (`manual` モード): `1.0` = 区画ぴったり。区画サイズに対する比率。NumberInput に加えて **0.1 刻みの +/- ボタン** で増減可能（1.0 未満の直接入力時のリセットバグを回避）
  - 画像回転: テキスト入力 + 上下ボタン（1° 刻み）。区画自体の回転とは独立した「画像の事前補正」
  - 画像選択 UI に **推奨サイズ表記** (最大 512×512 px・200 KB 以下、`.list.json` 埋め込みのため小さい画像推奨)
  - 区画リストの新規作成・読み込み・保存・**デフォルトに戻す**
  - **デフォルト区画も編集可能**（保存しない場合は次回起動時に失われる旨を画面で明示）
- ユーザー設定パネル（モーダル）: スナップ有効/無効、スナップサイズ（m）。共通コンポーネント [UserSettingsModal](src/shared/components/UserSettingsModal.tsx) を使用
- ナビゲーションガード: 未保存編集がある状態でメニュー等へ離脱しようとすると確認モーダル（`useBlocker`）
- 区画 1 つの描画は [ZonePreview](src/tools/editor/ZonePreview.tsx) と共通コンポーネント [ZoneImageKonva](src/shared/components/ZoneImageKonva.tsx) でレイアウトと揃っている

### 5.4 区画印刷 (`PrintPage`)

- 区画リストから印刷用 HTML を生成
- 設定項目:
  - 区画リストソース: 共有 IndexedDB / `.list.json` ファイル読込（accept フィルタ `.list.json`）/ デフォルトに戻す
  - 用紙サイズ: A3 / A4 / B5 / 任意指定（幅 cm, 高さ cm）
  - 向き: 縦 / 横
  - 印刷マージン: 上下左右（mm 単位、ユーザー指定）
  - 縮尺: a) 図面上の長さ（m / cm をドロップダウンで選択）, b) 印刷上の長さ（cm 固定）→ 縮尺を自動計算
  - 各区画の印刷枚数指定（区画ごとの `NumberInput`）
  - 区画名のグローバル表示有無（個別 `showName` と AND を取る）
  - 配置方式: `単純左上から敷き詰め` / `同種でグルーピング` / `1 種類 1 ページ`
- **永続化**: 用紙設定 (`paper`) と縮尺 (`scale`) は LocalStorage キー `evac-tool/print/settings` に保存。マージン・枚数・配置方式は永続化対象外（毎回の作業時に再設定する想定）
- **ページヘッダ方式**: 各ページの上部に固定領域 (`PAGE_HEADER_HEIGHT_MM = 10mm`) を確保し、
  - 左寄せ: 区画リスト名
  - 右寄せ: 縮尺表記（例: `図面 1 m → 印刷 5 cm`）
  - 下端罫線とページ番号フッタ
- 区画はメタ情報を**ヘッダに集約**し、各区画内は色塗り + 画像 + 枠線 + (オン時)区画名のみ
- **区画 SVG 化（「通常サイズで描画して縮尺で配置」方式）**: 各区画を `<svg viewBox="0 0 (width*100) (height*100)" width="${printedWmm}mm" ...>` で描画。テキスト・画像の表示サイズが印刷縮尺に追従して自動拡大縮小される
  - 区画表示名の font-size: `nameFontSize` (m) が指定されていれば `nameFontSize × 100` viewBox 単位、未指定時は `min(W,H) × 12%`（下限 4 単位）
  - 区画表示名の配置: `nameAlignH` / `nameAlignV` を SVG の `text-anchor` (`start` / `middle` / `end`) と `dominant-baseline` (`hanging` / `central` / `text-after-edge`) ＋ x/y 座標へマッピング
  - 画像は SVG `<image>` で `preserveAspectRatio` (fill: `none`, contain: `xMidYMid meet`) と SVG `transform="rotate(...)"` で実装
  - 枠線は SVG 内で**画像の上**に重ねた fill なしの `<rect stroke=...>` として描画。線幅は `(borderWidthPx × 0.265mm / mmPerMeter × 100)` viewBox 単位で計算し、印刷縮尺によらず実寸 mm 厚を維持
- エラー処理: 区画 1 つでも印刷可能領域（用紙 − マージン − ヘッダ高）より大きい場合、その区画名と寸法を列挙したエラーを表示し、生成を中断
- 出力: 単一の `.html` ファイル（CSS と画像 base64 をインライン、SVG ベース）。ユーザーがブラウザで開いて `Ctrl+P` で印刷する想定。`@page` で用紙サイズと向きを指定する。

## 6. 既知の制約と将来案

### 6.1 区画リストファイル形式の代替案（zip）

現状は **JSON 単一ファイル + base64 画像埋め込み**を採用する。

#### 想定される問題

- 区画画像が多くなるとファイルサイズが肥大化（数 MB ～ 数十 MB）
- base64 化により JSON サイズが画像バイナリの約 1.33 倍に膨らむ
- ブラウザの File API でロード時にメモリ消費が増える

#### 代替案: zip 形式（`.list.zip`）

- `list.json` に区画メタデータと画像参照（相対パス）を持つ
- `images/<image-id>.png` 形式で画像を別ファイルとして格納
- 全体を zip でまとめて単一ファイルとして配布

#### 採用判断のトリガー

実運用で以下のいずれかが観測された場合、zip 形式への移行を検討する。

1. 標準的な区画リスト（10 〜 20 区画）のファイルサイズが 5MB を超える
2. ファイル読み込み時に体感できる遅延（1 秒以上）が発生する
3. ユーザーからファイル管理の不満が出る

#### 移行コスト見積もり

- 追加ライブラリ: `jszip`（MIT, ~100KB）
- 改修範囲: `shared/zoneListIO.ts` のみ。データモデルは不変
- 旧 `.list.json` の読み込み互換: バージョンフィールドで判別し当面サポート

### 6.2 ブラウザ互換性

- 主要対応: Chrome / Edge / Firefox / Safari 最新版
- 印刷 HTML の `@page` 指定はブラウザ間で若干差異あり（向き指定の動作差）→ ユーザーが印刷ダイアログで確認する前提

## 7. 外部ライブラリ一覧

| ライブラリ           | バージョン | ライセンス | 用途                          |
| -------------------- | ---------- | ---------- | ----------------------------- |
| react                | 19.x       | MIT        | UI フレームワーク             |
| react-dom            | 19.x       | MIT        | DOM レンダリング              |
| react-router-dom     | 7.x        | MIT        | ルーティング（HashRouter）    |
| @mantine/core        | 8.x        | MIT        | UI コンポーネント             |
| @mantine/hooks       | 8.x        | MIT        | UI 連動フック                 |
| konva                | 10.x       | MIT        | キャンバス描画                |
| react-konva          | 19.x       | MIT        | konva の React バインディング |
| use-image            | 1.x        | MIT        | konva 用画像ローダー          |
| idb-keyval           | 6.x        | Apache-2.0 | IndexedDB ラッパー            |
| vite                 | 7.x        | MIT        | ビルドツール                  |
| typescript           | 5.x        | Apache-2.0 | 型システム                    |
| @vitejs/plugin-react | 5.x        | MIT        | Vite の React 連携            |
| eslint               | 9.x        | MIT        | リンタ                        |

依存ライセンスは原則 MIT または Apache-2.0。プロジェクト本体の MIT と整合。

---

## 8. 2026-06 機能拡張設計

> 2026-06-05 追記。避難所レイアウトツールに以下の機能群を追加する。
> - レイアウト画面からの区画編集（モーダル区画エディタ）
> - 居住者の性質（種類は拡張可能）と区画ごとの人数設定
> - ゾーン（多角形範囲）と、ゾーン内・階層・マップ全体の居住者カウント
> - 複数階層の施設（階層ごとの画像・縮尺）
> - マルチ操作（同時編集）の方式検討（実装は本フェーズ対象外）
>
> **文字列の配置（テキストブロック）は現行版に実装済み**（[src/tools/layout/AddTextModal.tsx](src/tools/layout/AddTextModal.tsx) / [EditTextModal.tsx](src/tools/layout/EditTextModal.tsx) / [Canvas.tsx](src/tools/layout/Canvas.tsx) の `TextItem`）。本フェーズでは新規実装せず、複数階層対応に伴う配置先の整理（§8.4）のみ行う。

### 8.1 データモデルの拡張

既存の型（§4）に対し、以下を追加・変更する。後方互換のため追加フィールドは原則オプショナルとし、レイアウトファイルはバージョンを `2` に上げる（version 1 は読込時に単一階層へ変換）。

#### 8.1.1 居住者の性質 (ResidentType)

```typescript
/** 区画を利用する居住者の性質。 */
type ResidentType = {
  id: string;        // UUID
  name: string;      // 例: "健常者"
  color: string;     // #RRGGBB（凡例・カウント表示の色分け用）
};

/** デフォルト 5 種類に加えてユーザーが追加できる種類の上限。 */
const MAX_ADDITIONAL_RESIDENT_TYPES = 3;
```

デフォルトの 5 種類（[src/shared/constants.ts](src/shared/constants.ts) に `DEFAULT_RESIDENT_TYPES` として定義）:

| id | 名前 | 既定色（案） |
| --- | --- | --- |
| `healthy` | 健常者 | 青系 |
| `needs-care` | 要配慮者 | 橙系 |
| `severe` | 重症者/重病人 | 赤系 |
| `staff` | 運営/自治体職員 | 緑系 |
| `volunteer` | ボランティア | 紫系 |

- **保持場所**: レイアウトファイル（`.layout.json`）に保持する（ユーザー回答による）。アプリ全体共有ではなく、レイアウトごとに種類定義を持つ。新規レイアウト開始時は `DEFAULT_RESIDENT_TYPES` で初期化する。
- **追加件数の制約**: デフォルトの 5 種類に加えて、ユーザーが追加できる種類は **最大 `MAX_ADDITIONAL_RESIDENT_TYPES`（= 3）件**（ユーザー回答による）。上限到達時は追加ボタンを無効化し、その旨を表示する。
- 編集 UI: レイアウト画面のツールバーから「居住者種類の設定」モーダルを開き、追加（上限内）・改名・色変更・削除・並べ替えを行う。
- **削除可否（ユーザー回答による）**: **デフォルト 5 種類は削除不可**。ただし **表示名（name）の変更と色変更は可**。**ユーザー追加分（最大 3 件）のみ削除可**。種類を削除する際、区画が人数を設定中であれば確認のうえ該当人数をクリアする。
- **既定色（ユーザー回答による）**: 配色は本ツール側で決定し（系統色案: 健常者=青 / 要配慮者=橙 / 重症者=赤 / 職員=緑 / ボランティア=紫）、**全種類について色をユーザーが変更可能**とする。

#### 8.1.2 区画への居住者設定 (PlacedZone 拡張)

```typescript
type PlacedZone = {
  kind: 'zone';
  id: string;
  typeId: string;
  x: number;
  y: number;
  rotation: number;
  width?: number;
  height?: number;
  /** 居住者の種類ID -> 人数。未定義・0 は「設定なし」。 */
  residents?: Record<string, number>;
};
```

- 人数は **配置済み区画インスタンスごと** に設定する（同じ区画タイプでも配置ごとに別人数）。
- 区画選択時に詳細パネル（またはモーダル）で種類別人数を入力する。
- 区画上には **居住者の合計人数** を表示する（種類別内訳は選択時のパネルで確認）。表示は区画表示名と干渉しない位置・スタイルで描画する（詳細は実装時に調整）。
- 定員チェック（面積に対する超過警告）は本フェーズでは対象外（将来案）。

#### 8.1.3 ゾーン（多角形範囲） (PolygonZone)

四角い「区画 (PlacedZone)」とは別概念の、3 点以上の多角形で囲む「ゾーン」。`PlacedItem` の判別共用体に `kind: 'polygon'` として追加する。

```typescript
type PolygonZone = {
  kind: 'polygon';
  id: string;
  /** 頂点（メートル単位）。3 点以上。 */
  points: { x: number; y: number }[];
  label?: string;
  /** 枠線の種類・色・太さ */
  strokeStyle: 'solid' | 'dashed' | 'dotted';
  strokeColor: string;       // #RRGGBB
  strokeWidthPx: number;
  /** 塗りつぶし色と透明度 */
  fillColor: string;         // #RRGGBB
  fillOpacity: number;       // 0.0 〜 1.0
  /** ゾーン内居住者カウントの表示有無 */
  showCount: boolean;
};

type PlacedItem = PlacedZone | TextBlock | PolygonZone;
```

- **作図フロー**: ツールバーの「ゾーン」ボタン → キャンバスを順次クリックして頂点を追加 → ダブルクリック / Enter / 始点クリックで確定（3 点未満の場合は確定不可）。Esc で作図キャンセル。
- 確定後は頂点ドラッグで形状変更、ゾーン全体ドラッグで移動、選択削除 / Delete で削除。
- 枠線種類・色・太さ、塗り色・透明度、カウント表示有無は選択時のパネル / モーダルで編集する。`fillOpacity` の既定は半透明（例: 0.3）。
- **重なり順（ユーザー回答による）**: ゾーンは **区画より上に半透明で重ねて** 描画する（塗り → 枠線 → カウントラベル の順）。区画が透けて見えるよう半透明前提。
- **ゾーン表示の ON/OFF（ユーザー回答による）**: ツールバーのトグルで **全ゾーンの表示 / 非表示を切り替えられる**（OFF 時はゾーンの塗り・枠線・カウントラベルを非表示にする。ゾーン自体のデータは保持）。

#### 8.1.4 居住者カウントの算定ルール

| カウント種別 | 算定対象 | ルール |
| --- | --- | --- |
| 区画の人数 | 単一 PlacedZone | `residents` の全種類合計 |
| ゾーン内人数 | PolygonZone | **その階層内で**、矩形（回転後の 4 頂点すべて）が多角形に **完全に内包される** 区画の人数合計（ユーザー回答による） |
| 階層人数 | Floor | その階層の全 PlacedZone の人数合計 |
| マップ全体人数 | LayoutFile | 全階層の人数合計 |

- 「完全に内包」の判定は、回転後区画の 4 隅すべてが多角形内部にあるか（point-in-polygon、ray-casting）で行う。**凹多角形では 4 隅内包＝完全内包を厳密保証しないため、その近似である旨を仕様書に明記する。**
- **複数ゾーンの重複（ユーザー回答による）**: 1 つの区画が複数のゾーンに完全内包される場合、**その区画は各ゾーンそれぞれのカウント対象に含める**（重複カウントを許容）。したがって全ゾーンのカウント合計が階層人数を超えることがある（仕様）。
- 種類別の内訳もゾーン / 階層 / 全体それぞれで集計可能とする（凡例の色で内訳表示）。
- カウントは派生値であり保存しない（表示時に都度計算）。

#### 8.1.5 複数階層 (Floor) とレイアウトファイル v2

```typescript
type Floor = {
  id: string;
  name: string;                  // 例: "1F" / "2F"
  scaleRatio: number;            // px per meter（階層ごとに設定可能）
  background?: { dataUrl: string; width: number; height: number };
  placed: PlacedItem[];          // 区画・テキスト・ゾーン
};

type LayoutFile = {
  version: 2;
  /** レイアウトが参照する区画リスト名（共有リスト保存時に追従して更新） */
  zoneListName: string;
  /** 居住者の種類定義（レイアウトに内包） */
  residentTypes: ResidentType[];
  /** 区画リスト本体（自己完結のため引き続き丸ごと内包） */
  zoneList: ZoneList;
  /** 階層。最低 1 つ。 */
  floors: Floor[];
};
```

- 各階層が **独立した背景画像・縮尺・配置物（区画/テキスト/ゾーン）** を持つ。
- 階層の切替は画面上部の **タブ** で行う（追加・改名・削除・並べ替え可）。
- `scaleRatio` と `background` / `placed` は階層ごとに移動。旧 `LayoutFile`（v1）の `scaleRatio` / `background` / `placed` はトップレベルから消え、`floors[0]` に格納される。
- **後方互換（v1 → v2 変換）**: `parseLayout` が `version === 1` を検出したら、`{ id, name:"1F", scaleRatio, background, placed }` の単一 `Floor` を生成し、`residentTypes` は `DEFAULT_RESIDENT_TYPES`、`zoneListName` は `zoneList.name` で補完する。`version === 2` は新スキーマで読む。
- LocalStorage の自動復元キー `evac-tool/layout/state` も v2 形式（floors 配列）へ移行する。

### 8.2 レイアウト画面からの区画編集（モーダル区画エディタ）

- レイアウト画面のツールバー / 区画パレット付近に **「区画追加」「区画編集」** ボタンを置き、**モーダルで区画エディタ相当の UI を開く**（ユーザー回答による）。既存の [EditorPage](src/pages/EditorPage.tsx) のコア（[useEditorState](src/tools/editor/useEditorState.ts) / [ZoneListPane](src/tools/editor/ZoneListPane.tsx) / [ZoneDetail](src/tools/editor/ZoneDetail.tsx)）をモーダル内に再利用し、二重実装を避ける。
  - そのため EditorPage の本体を「ページ枠」と「エディタ本体コンポーネント」に分離し、ページ・モーダル双方から使えるようにリファクタする。
- **共有区画リストへの反映と保存（ユーザー回答による）**:
  - モーダルで編集した区画リストは **共有区画リスト（IndexedDB `evac-tool/zone-list`）として保存できる**。
  - **保存時にリスト名（ファイル名）を変更できる**。名前を変更して保存した場合、**そのレイアウトが参照する区画リスト名（`LayoutFile.zoneListName` および内包する `zoneList.name`）も連動して更新する**。
  - **未保存の編集がある状態でレイアウトを離脱・終了しようとした場合は警告を出す**（保存 / 破棄 / キャンセルの確認モーダル）。レイアウト画面自体の `useBlocker` ナビゲーションガードに、区画リスト未保存状態を統合する。

### 8.3 居住者カウントの表示

- ステータスバー（[src/tools/layout/StatusBar.tsx](src/tools/layout/StatusBar.tsx)）またはサイドの集計パネルに、
  - **現在の階層の人数合計**
  - **マップ全体（全階層）の人数合計**
  をユーザー回答に基づき表示する。種類別内訳は凡例の色とともに展開表示する。
- ゾーン内人数は、各 PolygonZone の `showCount === true` のときキャンバス上のラベルとして表示する（ゾーン中心付近）。

### 8.4 描画・配置レイヤの整理（Canvas）

[Canvas.tsx](src/tools/layout/Canvas.tsx) の描画レイヤを以下の重ね順に整理する（下→上）:

1. 背景画像
2. 区画（色塗り → 画像 → 枠線 → 表示名 → 居住者人数）
3. テキストブロック
4. ゾーン（半透明の塗り → 枠線 → カウントラベル）※ゾーン表示トグルが ON のときのみ
5. 縮尺キャリブレーション / 作図中ガイド / Transformer

ゾーンは区画より上に半透明で重ねる（§8.1.3）。ゾーン表示トグルが OFF のときはレイヤ 4 を描画しない。
`placed` は階層ごとに分離されたため、Canvas は「現在の階層の placed」だけを描く。

### 8.5 ストレージキーの変更点

| キー | 変更 |
| --- | --- |
| `evac-tool/layout/state` | v2 形式（`floors`・`residentTypes`・`zoneListName` を含む）へ移行 |
| `evac-tool/zone-list` | 変更なし（モーダル区画エディタからの保存先として共用） |

---

## 9. マルチ操作（同時編集）の方式検討

> 本フェーズは **調査・設計のみ**（実装対象外）。ユーザー回答による。

### 9.1 現状の制約

本ツールは **サーバを持たない完全クライアント側 SPA**（GitHub Pages 静的ホスティング、永続化は IndexedDB / LocalStorage）。Google Docs のようなリアルタイム同時編集には、**状態を共有・同期するバックエンド**が新たに必要となり、静的ホスティング前提から外れる。

### 9.2 採用方針：LAN 内ホスト・ハブ型（2026-06 決定）

ユーザー回答により、以下の方針を採用する。**GitHub Pages 版にはこの機能を含めない**（別ビルド）。

| 決定事項 | 内容 |
| --- | --- |
| ネットワーク範囲 | **同一 LAN 内のみ**（避難所・会議室など同一拠点での訓練利用）。NAT 越え不要、TURN 不要。 |
| アプリ提供形態 | **ホストがコラボ有効ビルドを LAN 公開**し、参加者はホストの URL をブラウザで開く |
| サーバ役 | **一人がホスト（サーバ役）**。ホストが軽量サーバを起動し、参加者が接続する星型（ハブ） |
| 同期方式 | **CRDT（Yjs）で自動マージ**（Google Docs 的な同時編集） |

#### トポロジの整理（WebRTC ではなく WebSocket ハブを採用する理由）

「LAN 内 ＋ ホストが軽量サーバを起動」という条件では、そのホストのサーバ自体を同期ハブにする
**Yjs + y-websocket（ホスト＝唯一のサーバ、参加者が WebSocket で接続）** が、「一人がサーバ役・他が参加」を最も素直に実現する。

- 厳密な WebRTC メッシュ（`y-webrtc`）は全ピアが相互接続する形で**単一ホストという概念が消える**（ホスト・ハブ型と逆）。
- 「ホストをハブにした WebRTC スター」は実装可能だが、LAN では中継ロジックを自作する割に利点が小さい。
- ホストが立てる「軽量サーバ」を **y-websocket サーバ（＝同期ハブ）** とすれば、シグナリングと中継を一体で賄え、最も簡潔・堅牢。

→ **データプレーンは WebSocket ハブ（y-websocket）を採用**。WebRTC は §9.10 に代替案として残す。

### 9.3 構成要素と技術スタック

ホスト機で動く軽量サーバ（`collab-server`, Node 製）が次の 2 役を担う。

1. **静的配信**: コラボ有効ビルド（`dist-collab/`）を HTTP で LAN 配信（例 `http://<ホストのLAN-IP>:5174/`）。
2. **同期ハブ**: `y-websocket` サーバとして WebSocket を待ち受け、Yjs の更新を全参加者へ中継（例 `ws://<ホストのLAN-IP>:5174/yjs`）。

| ライブラリ | 用途 | ライセンス |
| --- | --- | --- |
| `yjs` | CRDT 本体（共有データ型 Y.Doc / Y.Array / Y.Map） | MIT |
| `y-websocket` | WebSocket プロバイダ＋参照サーバ実装 | MIT |
| `y-protocols`（awareness） | プレゼンス（カーソル・選択・参加者情報）の共有 | MIT |
| （任意）`y-indexeddb` | 各クライアントのローカルキャッシュ（再接続時の高速復元） | MIT |
| （任意）`y-leveldb` | ホスト側のクラッシュ復旧用スナップショット | MIT |

> ホストは Node 実行環境が必要（`collab-server` を起動）。将来的に配布を簡便化するなら、`collab-server` を単一実行ファイル化（pkg/Bun/Deno compile 等）も検討。

### 9.4 データモデルの Yjs 化

現行の単方向ステート（`useLayoutState`）の正本を **Y.Doc** に置き換え、React へはバインディング層で反映する（Yjs 変更 → `setState`、ローカル操作 → Yjs 変異）。

```text
Y.Doc
├─ meta            : Y.Map        // zoneListName, residentTypes, zoneList, activeFloorId など
├─ floors          : Y.Array<Y.Map>   // 各階層
│    └─ (floor)    : Y.Map        // id, name, scaleRatio, background, placed
│         └─ placed: Y.Array<Y.Map>   // 区画/テキスト/ゾーンを 1 要素 = 1 Y.Map で保持
└─ （awareness は Y.Doc 外の awareness プロトコルで管理）
```

- **粒度**: 配置物（区画・テキスト・ゾーン）を個別の `Y.Map` にすることで、**別々の要素を同時に動かしても自動マージ**される。同一要素の同一プロパティ同時編集は CRDT の last-writer-wins。
- **背景画像（base64）**: LAN 内のため Yjs に載せて全参加者へ複製してよい。肥大化が問題なら、背景のみ HTTP 取得（ホスト配信）に分離する案を将来検討。
- **区画リスト / 居住者種類**: 更新頻度が低いため `meta`（Y.Map）に保持。区画リスト編集は粒度を細かくせず、まとめて差し替える運用で可。
- 既存の `LayoutFile`(v2) ⇄ Y.Doc の相互変換関数を用意（セッション開始時のシード、保存時のスナップショット）。

### 9.5 セッションのライフサイクルと参加フロー

- **ホスト開始**: コラボビルドを起動 → 「セッションを開始（ホスト）」→ `collab-server` 起動（または既起動を検知）→ ルーム ID 発行 → 参加 URL（`http://<LAN-IP>:5174/?room=<id>`）＋任意のパスコードを表示（QR 表示も検討）。既存 `.layout.json` を読み込んで Y.Doc をシード可能。
- **参加**: 参加者がブラウザで参加 URL を開く → 表示名（と色）を入力 → ルームへ join → Yjs が初期同期。
- **切断・再接続**: y-websocket が自動再接続。`y-indexeddb` 併用で再接続時の体感を改善。
- **ホスト終了**: セッション終了でルームを閉じる。ホスト機が落ちると同期は停止する（LAN・単一ホストの制約。§9.12 参照）。

### 9.6 プレゼンス（カーソル・選択の共有）

- `awareness` で各参加者の **表示名・色・カーソル位置（メートル座標）・選択中の要素・表示中の階層** を共有。
- 他参加者のカーソルと選択ハイライトを Canvas 上に色分け表示。
- 同一要素を複数人が掴んだ場合の視覚フィードバック（誰が操作中か）を表示。

### 9.7 権限・セキュリティ（LAN 前提）

- **ロール**: ホスト（セッション管理・キック・ロック）/ 編集者 / 閲覧者。
- **入室制御**: ルーム **パスコード**（任意）。必要ならホストが入室を承認するフローも追加可能。
- **通信**: LAN 内のため既定は `ws://`（非 TLS）。訓練用途では許容。TLS 化は自己署名証明書の配布が要るため当面非対応とし、ドキュメントに明記。
- y-websocket は既定で認証を持たないため、ルーム ID をパス・パスコードで保護し、サーバ側でルーム名検証を行う薄い実装を追加する。

### 9.8 永続化

- **保存**: ホストが任意のタイミングで現在の Y.Doc を既存 `.layout.json`(v2) としてダウンロード保存（既存の `buildLayoutFile` を流用）。
- **復旧**: 任意で `y-leveldb` によりホストにスナップショットを保持し、セッション再開時に復元。
- 参加者はローカルに `y-indexeddb` キャッシュを持つが、正本はあくまでホストのセッション。

### 9.9 ビルド分離（GitHub Pages 版から除外）

- コラボ機能はビルド時フラグで切り替える（例 `VITE_COLLAB` 環境変数、または専用エントリ）。
- **GitHub Pages ビルド**: `VITE_COLLAB` 無効。Yjs/コラボ系コードと UI は**動的 import ＋フラグ**で分岐し、tree-shaking でバンドルから除外。
- **LAN コラボビルド**: `npm run build:collab`（仮）で `VITE_COLLAB=true`。`collab-server`（Node）と合わせてホストが配布・起動。
- 既存のローカル機能（IndexedDB 保存等）はコラボ無効時もそのまま動作。

### 9.10 代替データプレーン（WebRTC）と不採用理由

- **WebRTC メッシュ（y-webrtc）**: シグナリングサーバのみで P2P。ただし全ピア相互接続で**ホスト・ハブの概念が消える**ため、今回の方針と不一致。
- **WebRTC スター（ホスト中継）**: ホストをハブにできるが、DataChannel の張り替え・中継・再接続を自作する必要があり、LAN では y-websocket に対する利点が乏しい。
- 将来、サーバを常駐させたくない／拠点をまたぎたい等の要件が出た場合に再検討する。

### 9.11 PoC・段階導入計画（実装は本フェーズ対象外）

1. **PoC1**: 同一 LAN の 2 ブラウザ間で、`y-websocket`（ホスト起動）経由に 1 階層の**区画移動を同期**。改修規模と体感を計測。
2. **PoC2**: フルデータモデル（複数階層・ゾーン・居住者人数・区画リスト）の Yjs 化と双方向バインディング。
3. **PoC3**: awareness によるカーソル/選択共有、参加 UX（URL＋QR・表示名・色）、ルームパスコード。
4. **PoC4**: ホストの保存/読込（`.layout.json` シード・スナップショット）、再接続・大きい背景画像の扱い、ビルド分離（§9.9）の確立。

### 9.12 留意点・未決事項

- **単一障害点**: ホスト機が落ちるとセッションが止まる（LAN・単一ホストの本質的制約）。ホスト移譲（別参加者がホストを引き継ぐ）は将来課題。
- **ホストの実行環境**: Node 実行が前提。配布簡便化（実行ファイル化）の要否は運用次第。
- **背景画像の同期コスト**: 階層数 × base64 サイズ次第で初期同期が重くなる可能性。HTTP 分離案の採否は計測後に判断。
- **同一要素の同時編集 UX**: last-writer-wins で許容できるか、ロック/プレゼンスでどこまで緩和するか。
- **既存 `useLayoutState` の Yjs 化**: 最大の改修ポイント。バインディング層を挟み、コラボ無効時は従来の `useState` 経路を使う二系統構成にするか、Yjs を常時正本にしてコラボ無効時はローカル単独 Doc にするか、を PoC2 で確定する。

### 9.13 セキュリティリスクと対策

> 2026-06-05 追記。本マルチ操作方式（LAN 内・ホストハブ・`y-websocket`・Yjs・既定 `ws://`）の設計レビュー。実装着手前に対策の採否を確定すること。

#### 脅威モデルの前提

最重要の前提誤りは **「LAN ＝信頼できる」という想定**。実運用の「LAN」は避難所・会議場の**共用/ゲスト Wi‑Fi** や来場者多数の社内ネットなど、**しばしば未信頼**である。以下は「同一ネットワーク上に悪意ある端末が居うる」前提で評価する。

#### リスク登録簿（深刻度 / 要因 / 対策）

**A. 通信路（`ws://` / `http://`・非 TLS）**

| # | リスク | 深刻度 | 要因 | 対策 |
| --- | --- | --- | --- | --- |
| A1 | 盗聴 | 高 | 平文配信。共用 Wi‑Fi で誰でもスニフ可能。図面・居住者数・氏名・パスコードが露出 | TLS 化（自己署名+配布 / mkcert）。または「機微情報を載せない」運用前提を明記 |
| A2 | MITM / なりすまし | 高 | サーバ認証が無い。ARP/mDNS スプーフでホスト偽装→悪意ある SPA 配信や更新改ざん | TLS+証明書固定。最低限ホスト指紋（IP/フィンガープリント）の口頭確認 |
| A3 | パスコードの平文露出・リプレイ | 高 | `ws://` でパスコード送信→捕捉・再利用 | TLS 必須化、チャレンジレスポンス化、ワンタイム化 |

**B. 認証・認可（最大の弱点）**

| # | リスク | 深刻度 | 要因 | 対策 |
| --- | --- | --- | --- | --- |
| B1 | 無認証参加 | 高 | y-websocket は既定で**認証もルーム別アクセス制御も無い**。ルーム名を知れば誰でも全文書を読み書き | サーバ側でルーム検証＋トークン必須化の薄い層を自作 |
| B2 | ルーム ID ＝事実上のベアラトークン | 高 | 推測可能・共有経路から漏れると全権取得。レート制限が無く総当たり可能 | 長い乱数 ID＋パスコード、接続レート制限、入室をホスト承認制に |
| B3 | ロールがクライアント任せ＝権限昇格 | 高 | リレー型は更新を素通しするため、改造クライアントの「閲覧者」が編集更新を送れば read-only を突破 | C1 の検証ゲートが無い限り read-only は保証不可。閲覧専用は別 Doc へ単方向ミラー等で実装 |
| B4 | 参加者なりすまし | 中 | awareness の表示名/色は自己申告 | 名前の真正性は保証しない旨を明記。必要なら入室時のみホストが識別 |
| B5 | キック/失効が効かない | 中 | クライアント側強制では追放後もルーム ID で再入室可能 | サーバ側セッション管理＋トークン失効。ルーム ID 再発行 |

**C. CRDT / データ完全性（Yjs 特有）**

| # | リスク | 深刻度 | 要因 | 対策 |
| --- | --- | --- | --- | --- |
| C1 | 更新の無検証＝任意改ざん/破壊 | 高 | 中継サーバは CRDT 更新を検証しない。悪意/バグのあるクライアントが文書全削除・不正値・構造破壊を注入でき、全員に自動マージ | 検証ゲートを挟む独自サーバ（適用前にスキーマ/権限チェック）。ただし CRDT では難度高（後述ジレンマ） |
| C2 | 共有文字列の XSS/インジェクション | 中 | テキストブロック・区画名・表示名・リスト名/ファイル名等が全員にレンダリングされる | Konva の Canvas テキストは安全だが、DOM 側（Mantine ラベル/ツールチップ/ファイル名）はエスケープ徹底。data:URL は画像 MIME・サイズ検証 |
| C3 | 文書肥大化によるメモリ DoS | 中 | CRDT はトンボストーン蓄積。生成/削除連打や巨大 base64 で全ピア＋ホストのメモリ圧迫 | 1 要素サイズ上限、操作レート制限、背景画像のサイズ上限・HTTP 分離 |
| C4 | 否認（誰が変更したか不明） | 低〜中 | 更新に署名/帰属が無い | 監査ログ（who/when/what）をホスト側に記録 |

**D. サービス妨害（DoS）**

| # | リスク | 深刻度 | 要因 | 対策 |
| --- | --- | --- | --- | --- |
| D1 | 接続フラッド/巨大メッセージ | 中 | 接続数・メッセージサイズ上限が既定で無い | 同時接続上限、メッセージサイズ上限、レート制限 |
| D2 | 単一悪意ユーザーで全員劣化 | 中 | リレー型は全員に伝播 | 疑わしいクライアントの強制切断（ホスト権限） |

**E. ホストサーバの攻撃面（個人 PC 上の Node）**

| # | リスク | 深刻度 | 要因 | 対策 |
| --- | --- | --- | --- | --- |
| E1 | 過大な公開範囲 | 高 | `0.0.0.0` バインドで意図より広いサブネットへ露出。FW の「パブリックで許可」誤クリック | 既定で特定 IF/サブネットに限定、起動時に公開範囲を警告 |
| E2 | 静的配信のパストラバーサル | 中 | 自作配信が素朴だとホスト任意ファイルを読み出し可能 | 実績ある静的配信を使い、配信ルートを厳格固定 |
| E3 | ポート/プロセス残留 | 中 | 終了忘れで露出が継続 | 明示終了 UI、アイドルタイムアウトで自動停止 |
| E4 | サーバ脆弱性からのピボット | 中 | ホスト主要 PC 上で動くため、サーバ侵害が個人ファイル/社内網へ波及 | 最小権限実行、専用ユーザ/サンドボックス、依存の更新 |

**F. クライアント / プライバシー**

| # | リスク | 深刻度 | 要因 | 対策 |
| --- | --- | --- | --- | --- |
| F1 | ローカルキャッシュ残留 | 中 | `y-indexeddb` 併用時、図面・人数等が各端末に残存（借用/共用端末で漏洩） | 終了時キャッシュ削除、キャッシュ無効オプション |
| F2 | 機微データの広域複製 | 中 | 実在避難所の図面・居住者内訳が全参加者へ複製・平文流通 | データ分類と運用ルール、TLS、参加者限定 |
| F3 | awareness 偽装/肥大 | 低 | プレゼンスも無検証 | サイズ上限・検証 |

**G. ビルド / サプライチェーン**

| # | リスク | 深刻度 | 要因 | 対策 |
| --- | --- | --- | --- | --- |
| G1 | コラボコードの Pages 混入 | 低〜中 | `VITE_COLLAB` 誤設定で公開ビルドに接続ロジック/既定 URL が混入 | 公開バンドルに yjs 等が無いことを CI で検証 |
| G2 | 依存増によるサプライチェーン | 中 | yjs/y-websocket 等と推移的依存。y-websocket 参照実装は本来デモ用 | 依存ピン留め・監査、参照サーバをそのまま本番化しない |
| G3 | 署名なし Node サーバ配布 | 低 | AV/SmartScreen 警告→利用者が保護を無効化 | 配布手順の明示、必要なら署名/実行ファイル化 |

**H. 運用・人的**

「LAN 内」前提の崩壊（ゲスト網=未信頼）、パスコードの安全でない共有、ホスト放置、セッション無期限。→ 運用ガイドラインで補う。

#### 本質的ジレンマ（設計レベルの要注意点）

**CRDT ＋リレー型サーバは「サーバ側の認可・検証」と相性が悪い** ことが最大の構造的弱点。

- y-websocket は基本「素通し中継」。**read-only の保証（B3）** と **更新の正当性検証（C1）** は素のリレーでは実現できない。
- 厳密にやるには、全更新を解釈・検証する**独自ゲートウェイ**（Yjs 更新を適用前にスキーマ/権限チェック）か、**閲覧者を別の単方向ミラー Doc に隔離**する設計が必要で、実装コストが増える。
- → 「ロール（編集者/閲覧者）を本当に強制するのか、紳士協定で十分か」を**スコープとして先に決める**べき。

#### 推奨ベースライン対策（最小構成）

1. **TLS を既定化**（mkcert 等の自己署名＋配布手順）。少なくともパスコードは TLS 下でのみ送る。
2. **サーバ側のルーム認証層**：長い乱数ルーム ID＋パスコード必須、接続レート制限、同時接続/メッセージサイズ上限。
3. **入力の検証/サニタイズ**：共有文字列・ファイル名・data:URL（画像 MIME・サイズ上限）。DOM 描画箇所のエスケープ徹底。
4. **公開範囲の最小化**：特定サブネット/IF のみ bind、起動時に公開範囲を警告、アイドル自動停止、明示終了。
5. **キャッシュ/データ運用**：終了時にローカルキャッシュ削除、機微情報の取扱いルール、監査ログ（who/when/what）。
6. **ビルド分離の検証**：公開バンドルにコラボ依存が無いことを CI チェック。

#### 残留リスクとスコープ論点

- TLS やサーバ認可を入れても、**改造クライアントによる権限内データ破壊（C1）** は CRDT の性質上ゼロにできない（バージョン履歴で復旧可能にするのが現実解）。
- **判断が必要な点**: ①ロール強制を本気でやるか（=検証ゲート実装の要否）、②TLS を必須にするか、③機微な実在図面を扱う想定か（=暗号化・端末残留対策の強度）。これらは PoC 前に決定する。

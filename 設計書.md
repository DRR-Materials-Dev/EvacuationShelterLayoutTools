# 避難所レイアウトツール 設計書

> 2026-05-11 オーバーホール版 / 仕様書とあわせて読むこと

## 1. 概要

避難所レイアウトツールを、独立した 3 つのツールの集合に再構成する。

- **避難所レイアウト**: 施設図面の上に区画を配置するツール（従来機能）
- **区画エディタ**: 区画と区画リストを編集・保存するツール（新規）
- **区画印刷**: 区画リストから印刷用 HTML を生成するツール（新規）

初期画面はメニュー画面で、そこから 3 つのツールに遷移する。

## 2. アーキテクチャ全体像

### 2.1 ルーティング

`react-router-dom` v7 の `HashRouter` を使用。GitHub Pages の静的ホスティング制約（直リンクで 404 になる問題）を回避するため。

```text
/#/             メニュー画面
/#/layout       避難所レイアウト
/#/editor       区画エディタ
/#/print        区画印刷
```

各ツール画面には「メニューに戻る」ボタンを配置する。終了ボタンは設けない。

### 2.2 永続化

| データ                             | 保存先                     | 理由                                             |
| ---------------------------------- | -------------------------- | ------------------------------------------------ |
| 共有区画リスト（画像 base64 含む） | IndexedDB (`idb-keyval`) | LocalStorage の 5MB 上限を超える可能性が高いため |
| ユーザー設定（スナップ等）         | LocalStorage               | 小さく頻繁に読まれるため                         |
| 避難所レイアウトの背景画像         | IndexedDB (`idb-keyval`) | 画像バイナリのため（仕様継続）                   |
| 配置済み区画情報・縮尺             | LocalStorage               | 軽量。次回起動時の自動復元用                     |

旧バージョンの LocalStorage / IndexedDB のキーは無視し、新フォーマットでクリーンスタートする。

### 2.3 ディレクトリ構成

```text
src/
  main.tsx                    エントリ
  App.tsx                     <HashRouter> + <Routes>
  pages/
    MenuPage.tsx              "/"
    LayoutPage.tsx            "/layout"
    EditorPage.tsx            "/editor"
    PrintPage.tsx             "/print"
  tools/
    layout/                   避難所レイアウト固有のコンポーネント
    editor/                   区画エディタ固有のコンポーネント
    print/                    区画印刷固有のコンポーネント
  shared/
    types.ts                  Zone / ZoneList / Layout の型定義
    zoneListIO.ts             .list.json の入出力
    layoutIO.ts               .layout.json の入出力
    storage.ts                IndexedDB / LocalStorage ラッパー
    components/               共通コンポーネント（ヘッダー、戻るボタン等）
    settings.ts               ユーザー設定の型と既定値
  styles/                     グローバルスタイル
```

### 2.4 言語

**TypeScript** を採用。長期メンテナンスを見据え、型による安全性と IDE 補完を確保する。`.jsx` → `.tsx`、`.js` → `.ts` に移行する。

## 3. UI ライブラリ採用方針

### 3.1 採用ライブラリ

**Mantine v8** を採用。

### 3.2 採用理由

| 評価軸                                         | Mantine | MUI | shadcn/ui | Chakra UI | Vanilla CSS |
| ---------------------------------------------- | ------- | --- | --------- | --------- | ----------- |
| 一般ユーザー向けの親しみやすい既定デザイン     | ◎      | ○  | ○        | ○        | △          |
| TypeScript ファースト                          | ◎      | ○  | ◎        | ○        | -           |
| バンドルサイズ（tree-shaking 後）              | ○      | △  | ◎        | ○        | ◎          |
| ダイアログ・モーダル・通知・数値入力等の網羅性 | ◎      | ◎  | △        | ○        | ×          |
| 設定パネル向け Form コンポーネントの充実       | ◎      | ○  | ○        | ○        | ×          |
| ライセンス                                     | MIT     | MIT | MIT       | MIT       | -           |

**決め手:**

1. メニュー画面・3 ツール画面・設定パネル・縮尺入力・印刷設定など、フォームとモーダルが多用される構成のため、Mantine の標準コンポーネント（`Modal`, `NumberInput`, `Select`, `ColorInput`, `FileInput`, `Stack`, `Group` 等）の網羅性が最も適合する。
2. MUI と比較してバンドルサイズが軽く、デザインも柔らかく一般ユーザー向きと判断。
3. shadcn/ui は Tailwind 前提でコピペ運用のため、本ツール規模では構築コストが見合わない。
4. Vanilla CSS 継続は、3 ツール × ダイアログ多数の構成で再発明コストが大きく、メンテナンス性で不利。

### 3.3 デザイントークン

Mantine の `MantineProvider` で、本ツール独自のプライマリカラー（青系 `#2196f3` 継承）と日本語向けフォントスタックを設定する。ライト/ダーク切替は当面なし。

## 4. データモデル

### 4.1 区画 (Zone)

```typescript
type ZoneType = {
  id: string;                  // UUID v4 推奨
  name: string;                // 例: "受付"
  width: number;               // メートル単位
  height: number;              // メートル単位
  color: string;               // 区画自体の塗り潰し色（#RRGGBB）
  resizable: boolean;          // 個別指定可
  /** 配置・印刷時に区画表示名を表示するか。未定義は true 扱い */
  showName?: boolean;
  /** 区画名と区画表示名を同じにするか。未定義は true 扱い。false なら displayName を使用 */
  displayNameSameAsName?: boolean;
  /** displayNameSameAsName が false のときに区画上へ描画される表示名。未定義は '' 扱い */
  displayName?: string;
  /** 表示名の文字色 (#RRGGBB)。未定義は '#000000' */
  nameColor?: string;
  /** 表示名のフォントサイズ（メートル単位、テキスト高さ）。未定義は自動（区画寸法に追従） */
  nameFontSize?: number;
  /** 表示名の横位置。未定義は 'center' */
  nameAlignH?: 'left' | 'center' | 'right';
  /** 表示名の縦位置。未定義は 'middle' */
  nameAlignV?: 'top' | 'middle' | 'bottom';
  /** 枠線を描画するか。未定義は true 扱い */
  showBorder?: boolean;
  /** 枠線色 (#RRGGBB)。未定義は '#333333' */
  borderColor?: string;
  /** 枠線の太さ（区画エディタ画面上での px）。未定義は 1 */
  borderWidthPx?: number;
  image?: {
    dataUrl: string;           // base64 画像（"data:image/png;base64,..."）
    fitMode: 'fill' | 'contain' | 'manual'; // 区画サイズに合わせる / 縦横比維持: 長辺フィット / 縮尺設定
    scaleX?: number;           // fitMode === 'manual' のとき有効（区画サイズに対する比、1.0 = ぴったり、0.1 刻みのボタン入力対応）
    scaleY?: number;
    rotation: number;          // -180 ～ 180、1° 刻み
  };
};
```

未定義フィールドは `getZoneBorder()` / `getZoneDisplayText()` / `getZoneNameColor()` / `getZoneNameAlignH()` / `getZoneNameAlignV()` ヘルパーでデフォルト値（[src/shared/types.ts](src/shared/types.ts) を参照）を補完。`.list.json` の旧バージョンとの後方互換のため。

### 4.2 区画リスト (`.list.json`)

```json
{
  "version": 1,
  "name": "標準区画リスト",
  "zones": [ /* ZoneType[] */ ]
}
```

- 拡張子: `.list.json`
- 画像は base64 で埋め込み（採用: Q9 案 A）
- 区画リストファイルを読み込むと、避難所レイアウトと区画印刷の区画一覧を置き換える

### 4.3 配置済み区画 (PlacedZone)

避難所レイアウト画面で配置された個別インスタンス。

```typescript
type PlacedZone = {
  id: string;                  // 配置 ID
  typeId: string;              // ZoneType.id
  x: number;                   // メートル単位
  y: number;
  rotation: number;            // 度
  // 個別リサイズの結果（resizable=true の場合のみ）
  width?: number;              // 上書き時のみ
  height?: number;
};

type TextBlock = {
  id: string;
  kind: 'text';
  text: string;
  x: number;
  y: number;
  fontSize: number;
  scaleX: number;
  scaleY: number;
  rotation: number;
  color: string;
};
```

座標・寸法は **メートル単位** で保持し、表示時のみ縮尺と viewScale を掛け算する。これにより縮尺再設定時にも既存配置が破綻しない（現状の px ベース実装の改善）。

### 4.4 レイアウトファイル (`.layout.json`)

```json
{
  "version": 1,
  "scaleRatio": 50,
  "background": {
    "dataUrl": "data:image/png;base64,...",
    "width": 1920,
    "height": 1080
  },
  "zoneList": { /* ZoneList 全体 */ },
  "placed": [ /* (PlacedZone | TextBlock)[] */ ]
}
```

- 拡張子: `.layout.json`
- 背景画像も base64 で埋め込み（採用: Q7）
- 使用した区画リストを丸ごと内包する（後日再現性確保のため）

### 4.5 ユーザー設定

```typescript
type UserSettings = {
  snap: {
    enabled: boolean;          // 既定 true
    sizeMeters: number;        // 既定 0.1（=10cm）
  };
};
```

LocalStorage キー: `evac-tool/user-settings`。

### 4.6 印刷ツールの設定永続化

```typescript
// 印刷ツール固有: 用紙設定と縮尺のみ永続化
type StoredPrintSettings = {
  paper: PaperSetting;   // size, orientation, customWidthCm, customHeightCm
  scale: ScaleSetting;   // drawingLength, drawingUnit, printLengthCm
};
```

LocalStorage キー: `evac-tool/print/settings`。マージン・枚数・配置方式は永続化対象外（毎回ユーザー設定する前提）。

### 4.7 ストレージキー一覧

| キー | 場所 | 内容 |
| --- | --- | --- |
| `evac-tool/zone-list` | IndexedDB | 共有区画リスト（最後に保存されたもの） |
| `evac-tool/background` | IndexedDB | 避難所レイアウトの背景画像 |
| `evac-tool/user-settings` | LocalStorage | スナップ設定 |
| `evac-tool/layout/state` | LocalStorage | 避難所レイアウトの縮尺と配置済みアイテム |
| `evac-tool/print/settings` | LocalStorage | 区画印刷の用紙設定と縮尺 |

「デフォルトに戻す」操作はそのツールが扱うキー（および共有キー `zone-list`）をクリアする。

## 5. 各ツールの仕様

### 5.1 メニュー画面 (`MenuPage`)

- タイトル「避難所レイアウトツール」と短い説明文を表示
- 3 つのツールをアイコン + 名称 + 説明の大きめのカードで配置
- 一般ユーザー向けの親しみやすいデザイン（Mantine の `Card` コンポーネント）

### 5.2 避難所レイアウト (`LayoutPage`)

- 従来機能を継承（画像読込、縮尺設定、ドラッグ配置、回転、リサイズ、ズーム、パン、PNG 保存）
- 追加機能:
  - 区画リストファイル (`.list.json`) 読み込み: 警告ダイアログ「配置されている区画が全て削除されますがよろしいですか？」→ OK で既存配置をクリアし、リストを置換
  - レイアウト結果保存: `.layout.json` で背景画像含めて出力（accept フィルタは `.layout.json`）
  - レイアウト読み込み: `.layout.json` から復元
  - 区画リスト初期化（デフォルトに戻す）: 配置済みも含めてクリア
  - ユーザー設定アクセス（ツールバーの「設定」ボタンから共通モーダルを開く）
- カスタム区画作成機能は **削除**（区画エディタへ統合）
- スナップ（ユーザー設定に従う）を配置・移動・リサイズ全てに適用
- **テキストブロック** のフロー: 「テキスト」ボタン → 配置先クリック → モーダルで文字列・フォントサイズ・**文字色** を入力して配置。既存テキストはダブルクリックで編集モーダルが開き、内容・サイズ・色を変更可能。
- 区画タイプに画像があれば、レイアウト上でも色塗り → 画像 → 枠線 → 文字 の順で重ねて描画する（[src/shared/components/ZoneImageKonva.tsx](src/shared/components/ZoneImageKonva.tsx) を経由）

### 5.3 区画エディタ (`EditorPage`)

- 区画リストを編集する独立ツール
- 機能:
  - 区画の追加・削除・複製
  - 区画ごとに以下を編集:
    - 名称、サイズ（m、スナップ適用）、塗り潰し色、リサイズ可否
    - **区画表示名関連**:
      - **区画表示名を表示する** (`showName`) — 区画上にテキスト描画するか
      - **区画名と区画表示名は同じ** (`displayNameSameAsName`) — OFF にすると区画名と別の `displayName` を指定可能
      - **表示名の文字色** (`nameColor`)
      - **文字サイズの自動調整 / 手動指定** (`nameFontSize`、メートル単位)
      - **横位置 / 縦位置** (`nameAlignH` / `nameAlignV`、各 3 値の SegmentedControl)
    - **枠線の有無 / 色 / 太さ (px)** — 白基調の区画など輪郭が見えにくい場合の補助
    - 画像、画像表示モード、画像縮尺、画像回転
  - 画像表示モード: ドロップダウン (`区画サイズに合わせる` / `縦横比維持: 長辺フィット` / `縮尺設定`)
  - 画像縮尺 (`manual` モード): `1.0` = 区画ぴったり。区画サイズに対する比率。NumberInput に加えて **0.1 刻みの +/- ボタン** で増減可能（1.0 未満の直接入力時のリセットバグを回避）
  - 画像回転: テキスト入力 + 上下ボタン（1° 刻み）。区画自体の回転とは独立した「画像の事前補正」
  - 画像選択 UI に **推奨サイズ表記** (最大 512×512 px・200 KB 以下、`.list.json` 埋め込みのため小さい画像推奨)
  - 区画リストの新規作成・読み込み・保存・**デフォルトに戻す**
  - **デフォルト区画も編集可能**（保存しない場合は次回起動時に失われる旨を画面で明示）
- ユーザー設定パネル（モーダル）: スナップ有効/無効、スナップサイズ（m）。共通コンポーネント [UserSettingsModal](src/shared/components/UserSettingsModal.tsx) を使用
- ナビゲーションガード: 未保存編集がある状態でメニュー等へ離脱しようとすると確認モーダル（`useBlocker`）
- 区画 1 つの描画は [ZonePreview](src/tools/editor/ZonePreview.tsx) と共通コンポーネント [ZoneImageKonva](src/shared/components/ZoneImageKonva.tsx) でレイアウトと揃っている

### 5.4 区画印刷 (`PrintPage`)

- 区画リストから印刷用 HTML を生成
- 設定項目:
  - 区画リストソース: 共有 IndexedDB / `.list.json` ファイル読込（accept フィルタ `.list.json`）/ デフォルトに戻す
  - 用紙サイズ: A3 / A4 / B5 / 任意指定（幅 cm, 高さ cm）
  - 向き: 縦 / 横
  - 印刷マージン: 上下左右（mm 単位、ユーザー指定）
  - 縮尺: a) 図面上の長さ（m / cm をドロップダウンで選択）, b) 印刷上の長さ（cm 固定）→ 縮尺を自動計算
  - 各区画の印刷枚数指定（区画ごとの `NumberInput`）
  - 区画名のグローバル表示有無（個別 `showName` と AND を取る）
  - 配置方式: `単純左上から敷き詰め` / `同種でグルーピング` / `1 種類 1 ページ`
- **永続化**: 用紙設定 (`paper`) と縮尺 (`scale`) は LocalStorage キー `evac-tool/print/settings` に保存。マージン・枚数・配置方式は永続化対象外（毎回の作業時に再設定する想定）
- **ページヘッダ方式**: 各ページの上部に固定領域 (`PAGE_HEADER_HEIGHT_MM = 10mm`) を確保し、
  - 左寄せ: 区画リスト名
  - 右寄せ: 縮尺表記（例: `図面 1 m → 印刷 5 cm`）
  - 下端罫線とページ番号フッタ
- 区画はメタ情報を**ヘッダに集約**し、各区画内は色塗り + 画像 + 枠線 + (オン時)区画名のみ
- **区画 SVG 化（「通常サイズで描画して縮尺で配置」方式）**: 各区画を `<svg viewBox="0 0 (width*100) (height*100)" width="${printedWmm}mm" ...>` で描画。テキスト・画像の表示サイズが印刷縮尺に追従して自動拡大縮小される
  - 区画表示名の font-size: `nameFontSize` (m) が指定されていれば `nameFontSize × 100` viewBox 単位、未指定時は `min(W,H) × 12%`（下限 4 単位）
  - 区画表示名の配置: `nameAlignH` / `nameAlignV` を SVG の `text-anchor` (`start` / `middle` / `end`) と `dominant-baseline` (`hanging` / `central` / `text-after-edge`) ＋ x/y 座標へマッピング
  - 画像は SVG `<image>` で `preserveAspectRatio` (fill: `none`, contain: `xMidYMid meet`) と SVG `transform="rotate(...)"` で実装
  - 枠線は SVG 内で**画像の上**に重ねた fill なしの `<rect stroke=...>` として描画。線幅は `(borderWidthPx × 0.265mm / mmPerMeter × 100)` viewBox 単位で計算し、印刷縮尺によらず実寸 mm 厚を維持
- エラー処理: 区画 1 つでも印刷可能領域（用紙 − マージン − ヘッダ高）より大きい場合、その区画名と寸法を列挙したエラーを表示し、生成を中断
- 出力: 単一の `.html` ファイル（CSS と画像 base64 をインライン、SVG ベース）。ユーザーがブラウザで開いて `Ctrl+P` で印刷する想定。`@page` で用紙サイズと向きを指定する。

## 6. 既知の制約と将来案

### 6.1 区画リストファイル形式の代替案（zip）

現状は **JSON 単一ファイル + base64 画像埋め込み**を採用する。

#### 想定される問題

- 区画画像が多くなるとファイルサイズが肥大化（数 MB ～ 数十 MB）
- base64 化により JSON サイズが画像バイナリの約 1.33 倍に膨らむ
- ブラウザの File API でロード時にメモリ消費が増える

#### 代替案: zip 形式（`.list.zip`）

- `list.json` に区画メタデータと画像参照（相対パス）を持つ
- `images/<image-id>.png` 形式で画像を別ファイルとして格納
- 全体を zip でまとめて単一ファイルとして配布

#### 採用判断のトリガー

実運用で以下のいずれかが観測された場合、zip 形式への移行を検討する。

1. 標準的な区画リスト（10 〜 20 区画）のファイルサイズが 5MB を超える
2. ファイル読み込み時に体感できる遅延（1 秒以上）が発生する
3. ユーザーからファイル管理の不満が出る

#### 移行コスト見積もり

- 追加ライブラリ: `jszip`（MIT, ~100KB）
- 改修範囲: `shared/zoneListIO.ts` のみ。データモデルは不変
- 旧 `.list.json` の読み込み互換: バージョンフィールドで判別し当面サポート

### 6.2 ブラウザ互換性

- 主要対応: Chrome / Edge / Firefox / Safari 最新版
- 印刷 HTML の `@page` 指定はブラウザ間で若干差異あり（向き指定の動作差）→ ユーザーが印刷ダイアログで確認する前提

## 7. 外部ライブラリ一覧

| ライブラリ           | バージョン | ライセンス | 用途                          |
| -------------------- | ---------- | ---------- | ----------------------------- |
| react                | 19.x       | MIT        | UI フレームワーク             |
| react-dom            | 19.x       | MIT        | DOM レンダリング              |
| react-router-dom     | 7.x        | MIT        | ルーティング（HashRouter）    |
| @mantine/core        | 8.x        | MIT        | UI コンポーネント             |
| @mantine/hooks       | 8.x        | MIT        | UI 連動フック                 |
| konva                | 10.x       | MIT        | キャンバス描画                |
| react-konva          | 19.x       | MIT        | konva の React バインディング |
| use-image            | 1.x        | MIT        | konva 用画像ローダー          |
| idb-keyval           | 6.x        | Apache-2.0 | IndexedDB ラッパー            |
| vite                 | 7.x        | MIT        | ビルドツール                  |
| typescript           | 5.x        | Apache-2.0 | 型システム                    |
| @vitejs/plugin-react | 5.x        | MIT        | Vite の React 連携            |
| eslint               | 9.x        | MIT        | リンタ                        |

依存ライセンスは原則 MIT または Apache-2.0。プロジェクト本体の MIT と整合。

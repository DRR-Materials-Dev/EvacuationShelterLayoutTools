# 避難所レイアウト検討ツール仕様書

> 実装設計の詳細は [設計書.md](設計書.md) を参照のこと。
> 2026-05 のオーバーホールで、当初の単一ツールから 3 ツール構成（避難所レイアウト / 区画エディタ / 区画印刷）へ再編した。

## 概要

避難所を運営する訓練として、居住区画や受け付け、物品置き場、授乳所、更衣所などの区画を実際の図面の上でレイアウトするツール。

## 構成

本ツールは、Web アプリとして作成する。メニュー画面から以下の 3 つの独立したツールに遷移できる:

- **避難所レイアウト**: 施設の図面上に区画を配置するメインツール
- **区画エディタ**: 区画と区画リスト (`*.list.json`) を編集・保存するツール
- **区画印刷**: 区画リストから印刷用 HTML を生成するツール

## 製作者

GitHubにデプロイすることで動作させることを想定している。
本ツールは他のプロジェクトと異なり、GitHubユーザーとしてGrandgeとしてコミットする。

## 機能

避難所となる施設の画像（pngを想定）を読み込み、ユーザーが図面の縮尺を図面の２点を指定することで扱う画像の縮尺を判断する。
施設の画像は区画と同じ寸法になるように拡大/縮小する。
画面はマウスのホイールやボタンで拡大縮小できる。
区画は最小単位が 10cm の寸法を持ち、配置する際は 360 度回転できる（スナップ単位はユーザー設定で変更可能）。
区画は、名前、寸法（縦、横）、イメージカラー、表示用画像（画像が無い場合もある）、**枠線設定（有無 / 色 / 太さ）**、**区画表示名関連の設定（表示有無 / 区画名と表示名を同じにするかのトグル / 別表示名 / 文字色 / フォントサイズ（自動 or メートル単位手動）/ 縦横の配置位置）** を持つ。
ユーザーは区画用に画像をアップロードできる（推奨: 最大 512×512 px・200 KB 以下）。
区画用にアップロードした画像の表示サイズ・縦横比・回転をユーザーが変更できる。
区画にはサイズ変更の可否設定があり、デフォルトで用意されている区画はサイズ変更不可とする（区画エディタで個別に変更可）。

### 文字列ブロック

区画とは別に、説明用の文字列を配置できる機能。

- 「テキスト」ボタンを押した後、配置先をクリックしてモーダルで文字列・フォントサイズ・**文字色** を入力する 2 段階フロー。
- 文字列ブロックはサイズ変更が可能。
- フォントサイズを変更可能。
- 配置後に文字列の内容と文字色をダブルクリックの編集モーダルで修正可能。
- 文字列ブロックの削除が可能（選択削除ボタンまたは Delete キー）。

施設画像の上に区画を配置する。
区画が重なった場合最後に置いた区画を上に表示する。

ユーザーが配置する区画には、居住区画（1x2, 2x2 複数種類）、受付、談話室、食堂、物資配布スペース、更衣室、受給コーナーなどがある。
ユーザーは **区画エディタ** で新たに区画の種類を作成・編集できる（避難所レイアウト側のカスタム区画作成 UI はエディタへ統合済）。
区画は位置を決めた後に回転できる。
一度置いた区画を移動できる。
一度置いた区画を回転できる。
一度置いた区画を削除できる。

配置エリアの画像をダウンロード（PNG）できる。
レイアウト全体（背景画像・縮尺・区画リスト・配置済みアイテム）を `*.layout.json` として保存・読込できる。

### 画面構成

- 画面最上部に「メニューに戻る」ボタンとツール名を表示するアプリヘッダ。
- その下に制御コマンドエリア（施設画像読込、縮尺設定、ズーム、テキスト、選択削除、全削除、区画リスト読込、レイアウト保存・読込、ユーザー設定、画像保存など）を配置する。
- 左側に配置する区画を選ぶための区画選択エリア（区画パレット）がある。
  - 配置可能な区画一覧を表示する
  - 全てを表示できない場合、縦スクロールバーで選択できる範囲を変更できる
- 画面下部に警告メッセージや状態（縮尺・表示倍率・配置数）を表示するステータスエリアがある。
- 中央は配置エリアと呼称する。このエリアにユーザーは区画を配置する。

### デフォルト区画

オーバーホール後の標準区画リスト ([src/shared/constants.ts](src/shared/constants.ts) で定義):

| 名前 | 幅 (m) | 高さ (m) | 色 |
| --- | --- | --- | --- |
| 居住スペース(小) | 1 | 2 | 黄 |
| 居住スペース(大) | 2 | 2 | 濃黄 |
| 受付 | 2.5 | 1.5 | 薄いオレンジ |
| 談話室 | 3 | 3 | 紫 |
| 食堂 | 4 | 3 | 桃 |
| 物資配布スペース | 2 | 1.5 | 青緑 |
| 更衣室 | 1 | 1.2 | 緑 |
| 受給コーナー | 1.8 | 0.9 | 水色 |

ユーザーは区画エディタで自由に追加・編集・削除し、`*.list.json` として保存できる。

## 2026-06 追加機能

> 実装設計は [設計書.md](設計書.md) §8・§9 を参照。

### レイアウト画面からの区画編集

- 避難所レイアウト画面に「区画追加」「区画編集」ボタンを設け、**区画エディタをモーダルで開いて**区画の追加・編集ができる（メニューへ戻らずに編集可能）。
- モーダルで編集した区画リストは **共有区画リストとして保存** できる。
- 保存時に **リスト名（ファイル名）を変更** でき、名前を変更して保存した場合は、そのレイアウトが参照する区画リスト名も連動して更新される。
- **区画リストに未保存の編集があるままレイアウトを離脱・終了しようとすると警告**（保存 / 破棄 / キャンセル）を表示する。

### 居住者の性質と人数

- 区画を利用する居住者の「性質（種類）」を扱う。デフォルトは **健常者 / 要配慮者 / 重症者・重病人 / 運営・自治体職員 / ボランティア** の 5 種類。
- これに加えて、ユーザーは **最大 3 件まで種類を追加** できる。各種類には表示用の色を設定できる。
- 種類定義はレイアウトファイル（`.layout.json`）に保持される。

#### 居住者の性質の編集画面

- レイアウト画面のツールバーから **「居住者種類の設定」画面（モーダル）** を開く。
- この画面で以下を操作できる:
  - **名称（表示名）の変更**: デフォルト 5 種類・追加分とも変更可。
  - **表示色の変更**: 全種類とも変更可（凡例・カウント表示の色分けに使用。既定色はツール側で設定）。
  - **並べ替え**: 表示順の変更。
  - **追加**: デフォルト以外に最大 3 件まで（上限到達時は追加不可）。
  - **削除**: **デフォルト 5 種類は削除不可**。**追加分のみ削除可**（人数設定中の種類を削除する場合は確認のうえ該当人数をクリア）。

#### 区画への人数設定

- **配置済みの区画ごとに、種類別の人数を設定** できる。区画上には合計人数を表示し、種類別の内訳は選択時のパネルで確認できる。

### ゾーン（多角形範囲）

- 区画とは別に、**3 点以上の多角形でゾーン（範囲）を作図** できる。
- ゾーンは **枠線の種類（実線 / 破線 / 点線）・色・太さ**、**塗りつぶし色・透明度** を指定できる。
- ゾーンは **区画の上に半透明で重ねて** 表示される。
- **ゾーンの表示は ON/OFF を切り替えられる**（OFF にすると全ゾーンを非表示。データは保持）。
- **ゾーン内に完全に内包される区画の居住者人数を集計** して表示できる（ゾーンごとに表示 / 非表示を切替可能）。
- **複数のゾーンが同じ区画を含む場合、その区画は各ゾーンそれぞれの人数カウント対象** となる（重複してカウントされる）。

### 居住者数のカウント表示

- **現在の階層の人数合計** と **マップ全体（全階層）の人数合計** を表示する。
- ゾーン内の人数カウントは、マップ全体・階層のカウントとは別に、各ゾーン上に表示する。

### 複数階層の施設

- 複数枚の施設画像を **同じ施設の各階層** として扱える。
- 各階層は **独立した背景画像・縮尺・配置（区画 / テキスト / ゾーン）** を持ち、画面上部のタブで切り替える。
- 階層ごとに縮尺を設定できる。
- レイアウトファイルは複数階層を含む新形式（version 2）で保存し、旧形式（単一画像）は単一階層として読み込める。

### マルチ操作（同時編集）の検討

- Google Docs のように複数ユーザーが同時に同じレイアウトを編集する機能について、**実現方式の調査・設計のみ**を行う（本フェーズでは実装しない）。実現にはサーバ（バックエンド）の追加が必要であり、候補方式と論点は [設計書.md](設計書.md) §9 に整理する。

## 2026-06 追加機能

> 実装設計は [設計書.md](設計書.md) §8・§9 を参照。

### レイアウト画面からの区画編集

- 避難所レイアウト画面に「区画追加」「区画編集」ボタンを設け、**区画エディタをモーダルで開いて**区画の追加・編集ができる（メニューへ戻らずに編集可能）。
- モーダルで編集した区画リストは **共有区画リストとして保存** できる。
- 保存時に **リスト名（ファイル名）を変更** でき、名前を変更して保存した場合は、そのレイアウトが参照する区画リスト名も連動して更新される。
- **区画リストに未保存の編集があるままレイアウトを離脱・終了しようとすると警告**（保存 / 破棄 / キャンセル）を表示する。

### 居住者の性質と人数

- 区画を利用する居住者の「性質（種類）」を扱う。デフォルトは **健常者 / 要配慮者 / 重症者・重病人 / 運営・自治体職員 / ボランティア** の 5 種類。
- これに加えて、ユーザーは **最大 3 件まで種類を追加** できる（追加分は改名・色変更・削除が可能）。各種類には表示用の色を設定できる。

#### 居住者の性質の編集画面

- レイアウト画面のツールバーから **「居住者種類の設定」画面（モーダル）** を開く。
- この画面で各種類について以下を編集できる:
  - **名称**（デフォルト 5 種類・追加分とも改名可）
  - **表示色**（凡例・カウント表示の色分けに使用）
  - **並べ替え**（表示順の変更）
  - **追加**（デフォルト以外に最大 3 件まで。上限到達時は追加ボタンを無効化）
  - **削除**（追加分のみ削除可。区画が人数を設定中の種類を削除する場合は確認のうえ該当人数をクリア）
- 種類定義はレイアウトファイル（`.layout.json`）に保持される。

#### 区画への人数設定

- **配置済みの区画ごとに、種類別の人数を設定** できる。区画上には合計人数を表示し、種類別の内訳は選択時のパネルで確認できる。

### ゾーン（多角形範囲）

- 区画とは別に、**3 点以上の多角形でゾーン（範囲）を作図** できる。
- ゾーンは **枠線の種類（実線 / 破線 / 点線）・色・太さ**、**塗りつぶし色・透明度** を指定できる。
- **ゾーン内に完全に内包される区画の居住者人数を集計** して表示できる（表示 / 非表示を切替可能）。

### 居住者数のカウント表示

- **現在の階層の人数合計** と **マップ全体（全階層）の人数合計** を表示する。
- ゾーン内の人数カウントは、マップ全体・階層のカウントとは別に、各ゾーン上に表示する。

### 複数階層の施設

- 複数枚の施設画像を **同じ施設の各階層** として扱える。
- 各階層は **独立した背景画像・縮尺・配置（区画 / テキスト / ゾーン）** を持ち、画面上部のタブで切り替える。
- 階層ごとに縮尺を設定できる。
- レイアウトファイルは複数階層を含む新形式（version 2）で保存し、旧形式（単一画像）は単一階層として読み込める。

### マルチ操作（同時編集）の検討

- Google Docs のように複数ユーザーが同時に同じレイアウトを編集する機能について、**実現方式の調査・設計のみ**を行う（本フェーズでは実装しない）。実現にはサーバ（バックエンド）の追加が必要であり、候補方式と論点は [設計書.md](設計書.md) §9 に整理する。

# コラボ「パッケージ／配信」ビルド用（build:collab → Electron が dist-collab を配信）。設計書 §9.9 / §9.15。
# VITE_COLLAB_WS_URL は指定しない：埋め込みサーバが HTTP 配信と WS ハブを同一オリジン（同一ポート）で
# 兼ねるため、クライアントは ws://<アクセス中のホスト:ポート> を自動使用する。
VITE_COLLAB=true
